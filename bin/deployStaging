#!/bin/bash

$CURRENT_BRANCH="$(git rev-parse --abbrev-ref HEAD)"

echo "Current branch: "
echo $CURRENT_BRANCH

heroku git:remote -a staging-chuckjoke -r staging
git push staging +$CURRENT_BRANCH:master
